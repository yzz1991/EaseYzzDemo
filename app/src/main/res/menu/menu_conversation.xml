<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    tools:context=".chat.ChatActivity">
    <item android:id="@+id/action_conversation_search"
        android:title="call"
        android:icon="@mipmap/action_light_search"
        app:showAsAction="ifRoom" />

    <item
        android:id="@+id/menu_add_chats"
        android:icon="@mipmap/action_new_chat"
        android:title="Add chat"
        app:showAsAction="always"
        >
        <menu>
            <item
                android:id="@+id/menu_create_group"
                android:title="创建群组"
                />
            <item
                android:id="@+id/menu_public_groups"
                android:title="公开群组"
                />
        </menu>
    </item>

</menu>